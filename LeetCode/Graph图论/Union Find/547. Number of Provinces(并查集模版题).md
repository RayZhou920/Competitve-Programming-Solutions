547.Number of Provinces 省会数量
====

https://leetcode.com/problems/number-of-provinces/
-----

![image](https://user-images.githubusercontent.com/91653378/140797361-8af0ccb2-40ab-40f2-a78f-3c0846a3e6f3.png)

代码：
----
````Java
// 并查集模版题
// 并查集专门解决连通性问题
// https://www.youtube.com/watch?v=Kn-nw7VbWak

class Solution {
    public int findCircleNum(int[][] isConnected) {
        UnionFind uf = new UnionFind(isConnected.length); // isConnected.length 指的是“我们有多少个节点”，也就是题目中所说的cities数量
        
        // base case
        if (isConnected == null || isConnected.length == 0) return 0;
        
        // 遍历二维矩阵
        for (int i = 0; i < isConnected.length; i++) {
            for (int j = 0; j < isConnected[i].length; j++) {
                if (isConnected[i][j] == 1) {
                    uf.union(i, j); // 本题中的i和j虽然是二维数组的下标，也指的是nodes，如果两个nodes在二维数组中对应的值均为1，说明这两个nodes是相邻的，那我们可以调用union方法将他们合并，从而count--，剩余的count就是我们要的答案
                }
            }
        }
        
        return uf.getCount(); // 调用getcount函数，数集合中有多少个元素
    }
    
    // 建立并查集，以及相关的方法
    private class UnionFind {
        int[] parent; //->代表每个元素的根
        int[] rank;
        int count; // -> number of components
        
        public UnionFind(int N) {
            parent = new int[N];
            rank = new int[N];
            count = N;
            for (int i = 0; i < N; i++) parent[i] = i;
        }
        
        public int find(int x) {
            while (parent[x] != x) {
                parent[x] = parent[parent[x]]; // 路径压缩
                x = parent[x];
            }
            return x;
        }
        
        public void union(int p, int q) {
            int pRoot = find(p);
            int qRoot = find(q);
            if (pRoot == qRoot) return;
            if (rank[pRoot] > rank[qRoot]) parent[qRoot] = pRoot;
            else if (rank[pRoot] < rank[qRoot]) parent[pRoot] = qRoot;
            else {
                parent[pRoot] = qRoot;
                rank[qRoot]++;
            }
            count--; //两元素相合，数目需要--；
        }
        
        public int getCount() { // -> 得到元素数量
            return count;
        }
    }
}
````
